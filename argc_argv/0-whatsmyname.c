#include "main.h"

/**
 * main - Imprime la poscion de argv
 * @argc: Dal la posicion de los elementos dentro de argv
 * @argv: Da la posicion de tod el string
 *
 * Return: cero
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
